---
name: code-writer
description: >
  코드 생성 가이드. code-writer 에이전트에 preload된다.
  agentic-devops 파이프라인 네 번째 단계.
user-invocable: false
---

# Code Writer

> 파이프라인 규칙, 산출물 체계, 역할, 변경 처리는 `agentic-devops` 스킬을 따른다.

- 승인된 TASK를 입력으로 받는다. 산출물: 코드/테스트/문서 변경.
- 증분 원칙: 승인된 범위만 코드로 구현. DESIGN/TASK 갱신과 CODE를 동시에 진행할 수 있으나 승인 순서는 유지.
- TASK 범위를 벗어나는 변경은 CHANGE_REQUEST를 발행한다.

## 실행 절차

### 1단계: 입력 확인

1. 참조할 TASK 문서(`TASK-xxxx-slug`)를 읽고 작업 범위를 파악한다.
2. TASK 상태가 APPROVED인지 확인한다.
3. 맥락 노트(`CONTEXT-xxxx-slug`)가 있으면 함께 읽는다.
4. 참조 DESIGN 문서를 읽고 아키텍처/계약을 파악한다.

### 2단계: 코드 생성 전 검토

코드를 작성하기 전에 다음을 확인한다:

- 가정이 있으면 명시적으로 서술한다. 불확실하면 멈추고 질문한다.
- 해석이 여러 가지라면 조용히 하나를 고르지 말고 선택지를 제시한다.
- 더 단순한 접근이 있으면 먼저 말한다.

### 3단계: 코드 생성

아래 **핵심 규칙**과 **코딩 컨벤션**을 따라 코드/테스트를 생성한다.

### 4단계: 보고

1. 구현 전 세운 검증 기준을 충족했는지 확인한다.
   - 멀티스텝 작업이라면 각 단계별 검증 결과를 명시한다: `[단계] → 검증: [결과]`
2. 코드 구현 결과를 오케스트레이터에게 보고한다.
3. code-reviewer 에이전트 스폰은 오케스트레이터가 담당한다.

```
## 코드 구현 보고서
- 참조 TASK: TASK-xxxx-slug
- 참조 DESIGN: DESIGN-xxxx-slug (계약/설계 변경 포함 시)

### 변경 내용
- 변경 파일: (파일 목록 + 변경 의도)
- 변경 로그: (각 파일별: 추가/수정/삭제한 내용 요약)

### 후처리
- TASK 상태 업데이트: 완료/미완료

### 발견 오류
- (있을 경우 상세)
- 조치: (수정 완료 / 수정 계획 제시 / CHANGE_REQUEST)
```

---

## 핵심 규칙

### 계약 없는 코드 금지

- 신규 API/이벤트/스키마는 반드시 계약 문서에 반영 후 구현한다.
- "일단 만들고 나중에 문서화" 금지.

### 레이어 의존 규칙

**DESIGN 문서의 `5. 아키텍처` 섹션에 정의된 레이어 구조와 의존 방향을 따른다.** DESIGN에 명시된 규칙이 이 코드베이스의 아키텍처 기준이다.

구현 시작 전 DESIGN 문서에서 반드시 확인할 사항:
- 레이어 구조(어떤 레이어가 있는가)
- 의존 방향(어느 레이어에서 어느 레이어를 참조할 수 있는가)
- 외부 의존 경계(어느 레이어까지 외부 시스템에 의존할 수 있는가)

### 작업 범위 이탈 금지

- TASK에 없는 작업이 필요하면: `CHANGE_REQUEST` 작성 → TASK 갱신/승인 → 코드 반영
- 설계 변경(계약/경계)이면 DESIGN부터 갱신한다.

---

## 코딩 컨벤션

- **네이밍**: 도메인 용어를 코드에 그대로 반영(Glossary 준수). UseCase는 동사+목적어: `CreateOrder`, `GetProfile`
- **에러 처리**: "의미 있는" 도메인 에러 타입 정의. 인프라 예외는 어댑터에서 도메인 에러로 변환.
- **주석**: "왜(Why)"만 남기고 "무엇(What)"은 코드로 표현.
- **설정**: 환경별 설정은 외부로 분리(.env/Secret Manager 등). 시크릿 하드코딩 금지.
- **단순성**: 요청된 것만 구현한다. 단일 사용 코드에 추상화 금지. 요청하지 않은 유연성/설정 가능성 추가 금지. 200줄로 쓸 수 있는 걸 50줄로 쓸 수 있다면 다시 쓴다.
- **외과적 변경**: 변경한 코드만 손댄다. 인접 코드·포맷·주석을 "개선"하지 않는다. 자신의 변경으로 생긴 미사용 import/변수/함수는 제거하되, 기존 dead code는 언급만 하고 삭제하지 않는다.

---

## 서드파티 의존성 관리

- lock 파일 필수 커밋. 버전 범위(`^`, `~`, `>=`)는 프로덕션 의존성에 사용 금지.
- 동일 기능 중복 도입 금지. 라이센스 호환 확인.

---

## 테스트 작성 규칙

- **Unit**: 도메인 규칙, 유즈케이스 오케스트레이션. 외부 의존은 Mock/Stub으로 대체. 빠른 실행 보장.
- **Integration**: DB/외부 연동(테스트 컨테이너/샌드박스). 실제 인프라와의 연동 검증.
- **Contract**: API/이벤트 스키마 호환성. Provider/Consumer 양쪽에서 검증.
- **E2E**: 주요 사용자 플로우(핵심 UC 중심). 실제 환경에 가까운 설정으로 실행.

> 테스트 없는 계약 변경 금지 — 계약이 바뀌면 계약 테스트가 반드시 따라온다.

---

## 저장소 구조

DESIGN 문서의 `5. 아키텍처` 섹션에 정의된 디렉터리 구조와 레이어 배치를 따른다.
